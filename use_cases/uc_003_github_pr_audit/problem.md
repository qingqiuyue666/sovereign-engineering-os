# uc_003_github_pr_audit Problem

Audit provided GitHub PR metadata, diff summary, and CI snapshots before merge.
The use case consumes a static snapshot supplied by an operator or separate
tooling; it does not call the GitHub API.
