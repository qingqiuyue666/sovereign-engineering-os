"""Narrow real-model tracer modules.

Tracers are out-of-band, narrowly-scoped probes that prove one real
provider-backed capability works against the existing system boundaries.
They are NOT part of the authority-bearing signable path. Tracers emit
their own honest audit records and make no replay-fidelity claims beyond
the adapter's declared ceiling (``"semantic"`` for real-provider output).
"""
