# uc_002_local_test_receipt Problem

Turn operator-provided local test command results into a receipt-shaped evidence
packet. The use case captures command identity, exit code, output digests, and
verifier status without introducing a command runner.
