# Pattern Assimilator Report Generator Runbook

## Command

```bash
python3 seos.py pattern report --json
```

The command reads local production runtime, package manifests, review artifacts, and repair ledger events. It writes `pattern_report.json` and `pattern_report.md` under `work/pattern_assimilator/`.
