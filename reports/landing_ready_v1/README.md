# Landing Ready Program V1 Reports

This directory stores repository-owned report schemas and release-readiness
decision artifacts for `CODEX_AUTONOMOUS_LANDING_READY_PROGRAM_V1`.

Runtime receipts generated during an autonomous landing run may live outside
the repository to keep `git status --short` clean during validation. Report
artifacts in this directory must cite those receipt paths and fail closed when
required evidence is missing.
