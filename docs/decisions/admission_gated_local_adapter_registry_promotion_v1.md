# Admission-Gated Local Adapter Registry Promotion v1

This decision records the #428 boundary for promoting only a #427 aggregation-bound local-fixture gate pass into a restricted local adapter registry record.

The promotion is not production promotion. It does not enable production, live websites, general browser automation, arbitrary URLs, account/login/registration flows, scraping, bypass/captcha workflows, secret access, cookie access, npm, npx, package installation, browser download, Playwright execution, or candidate repository code execution.

It does not enable live websites. It does not enable general browser automation. It does not enable arbitrary URLs. It does not enable account/login/registration flows. It does not enable scraping. It does not enable bypass/captcha.

It does not access secrets/cookies. It does not run npm/npx/install/browser download. It does not execute Playwright. It does not execute candidate repository code. It does not grant autonomy.

The promotion does not execute #422, does not execute #424, does not execute #425, does not execute #426, and does not execute #427. It reads an existing #427 admission gate decision artifact and validates the embedded #425 aggregation evidence fields and #426 regression evidence fields that #427 already bound into the decision.

The only allowed resulting state is:

- local_fixture_only
- human_review_required
- aggregation_bound
- non_production

The registry output is a local-fixture-only record. It does not grant autonomy, automatic approval, adapter auto-adoption, production adapter status, live website admission, external network admission, arbitrary navigation, account workflows, scraping, bypass, captcha, credential input, cookie access, package installation, browser download, npm/npx use, candidate repository access, candidate code import, candidate code execution, or arbitrary command execution.

Future live website work remains blocked by separate policy, legal, network, credential, and human-approval gates.
